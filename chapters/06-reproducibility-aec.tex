\chapter{Reproducibility and artifact evaluation}
\label{ch:repro}

\begin{objectives}\small
\begin{itemize}[nosep,leftmargin=*]
  \item relate artifact evaluation and the ACM badges to Software Heritage;
  \item verify an artifact's \textsc{swhid} offline, as a reviewer would;
  \item handle a double-blind submission with a directory \textsc{swhid}.
\end{itemize}
\end{objectives}

How much of computational science can actually be re-run? It is an uncomfortable question, and the
honest answers are sobering --- which is why our community has built a process to force the issue:
\emph{artifact evaluation}, now standard at computer-science conferences, checks that the software
behind a paper actually exists, runs, and supports its claims. It is the sharpest test of
everything these notes have argued for, and it turns out that Software Heritage answers its central
need almost perfectly. \textsc{ardc}, in other words, was the preparation; this chapter is the exam.

\takeaway{Artifact evaluation is the question this whole note was answering.}

\section{The reproducibility crisis}

Begin with the finding nobody likes to quote. When Collberg and Proebsting went looking for the code behind
several hundred papers from top computer-systems conferences, they found that, for a large majority,
the code could not be obtained, or could not be built~\autocite{Collberg2016}. Not refuted ---
simply \emph{unavailable}, or unbuildable, and so impossible to check at all. You cannot reproduce
what you cannot run; and, as the free-software tradition has always insisted, you cannot truly
inspect what you cannot read. A result resting on code is only as solid as our ability to get
\emph{that} code, at \emph{that} version, and run it again. A \textsc{swhid} secures the first half
of that sentence --- the \emph{identity} of the source, exactly; getting the \emph{same build} out
of it, bit for bit, is a separate (and harder) problem, the province of reproducible-build systems
such as Guix and Nix.\footnote{\url{https://reproducible-builds.org/}.} The two are complementary:
an intrinsic identifier pins what you built from, a reproducible build pins what you get. The whole
apparatus of artifact evaluation exists to push back on both fronts.

\section{Artifact evaluation and badging}

Artifact evaluation began, fittingly, as a contest: at ESEC/FSE 2011, where the first prize went to
Vouillon and Di Cosmo. What started as an experiment is now a near-universal standard, codified by
the ACM \emph{Artifact Review and Badging} policy~\autocite{acmbadging}, which defines three
independent families of badge (Figure~\ref{fig:badges}):

\begin{description}[leftmargin=!,labelwidth=3.6cm,font=\bfseries\sffamily]
  \item[Artifacts Available] the artifacts are placed in a publicly accessible archival repository,
        with a unique identifier.
  \item[Artifacts Evaluated] the artifacts are documented, consistent, complete, and exercisable
        (\emph{Functional}) --- and, a notch higher, structured carefully for reuse
        (\emph{Reusable}).
  \item[Results Reproduced] the paper's results were independently obtained by others, with the
        authors' artifacts (\emph{Reproduced}) or without them (\emph{Replicated}).
\end{description}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=.68\linewidth]{figures/acm-artifact-badges.png}
  \caption[The ACM artifact badges]{The ACM \emph{Artifact Review and Badging} badges. The
  \emph{Artifacts Available} badge --- a public archival repository plus a unique identifier --- is
  the one Software Heritage satisfies most literally.}
  \label{fig:badges}
\end{figure}

The practice has swept the field --- programming languages, software engineering, systems, security,
databases, high-performance computing --- to the point of routine: at EuroSys 2025 some 93\% of
papers earned the \emph{Functional} badge, and nearly half pursued \emph{Results Reproduced}. Some
communities go further still: in computer graphics, the \emph{Graphics Replicability Stamp} awards a
public seal only to papers whose code has been independently built and run to reproduce the results
--- hundreds of papers now carry it (Figure~\ref{fig:stamp}).\footnote{\url{http://www.replicabilitystamp.org/}.}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=.55\linewidth]{figures/replicabilitystamp.png}
  \caption[The Graphics Replicability Stamp]{The Graphics Replicability Stamp: an independent
  reviewer rebuilds and runs a paper's code, and only on success is the paper awarded the public
  stamp --- reproducibility certified by re-execution, not by author assertion.}
  \label{fig:stamp}
\end{figure}

\subsection{Artifact evaluation beyond computer science}

And the idea no longer stops at computer science. The same uncomfortable question ---
\emph{can the result actually be re-run?} --- is now being put, in each discipline's own
idiom, far outside our field; and, tellingly, the answers are hardening into standing
institutions rather than one-off favours to a reviewer.

Economics moved first, and moved hardest. Since 2019 the American Economic Association has
subjected \emph{every} empirical paper accepted at its journals to a mandatory code-and-data
audit, run before publication by a dedicated Data Editor --- not a badge an author may opt
into, but a gate the paper must pass~\autocite{aeadataeditor}. Beside that public mandate sits
a non-profit service, \emph{cascad}, which independently re-executes the code behind a study
and issues a reproducibility certificate; it has signed off on more than 170 of them to
date~\autocite{cascad}.

Other fields have built instruments of their own. In the life sciences and computational
geography, \emph{CodeCheck} pairs each submission with an independent \emph{codechecker} who
re-runs the analysis and issues a citable certificate, in partnership with venues such as
\emph{GigaScience} and the Reproducible AGILE conference~\autocite{codecheck}. Image processing
sets perhaps the most exacting bar of all: \emph{IPOL} publishes each algorithm together with a
peer-reviewed reference implementation and a runnable in-browser demo, so that a reader
reproduces the result in the very act of reading the paper~\autocite{ipol}. And security has
joined at the level of policy --- USENIX Security made artifact availability \emph{mandatory} in
2025~\autocite{usenixsec2025artifacts}.

Two efforts aim past any single discipline. \emph{ReScience~C} publishes independent
re-implementations that reproduce a paper's findings from scratch~\autocite{resciencec}, and
NISO's RP-31:2021 lifts the whole badging vocabulary out of computing into a discipline-neutral
recommended practice~\autocite{nisorp31} (Table~\ref{tab:beyond-cs}). Across every one of these
the instrument differs, but the need underneath is identical: a durable, publicly resolvable,
\emph{verifiable} handle on the exact code that was checked. Intrinsic identification does not
care which discipline minted the artifact --- a \textsc{swhid} names bytes, not a field --- which
is precisely why the same \textsc{ardc} discipline serves an economist, a geographer and a systems
researcher without changing a line.

\takeaway{Reproducibility policy has outgrown computer science --- but the handle it needs is the
same everywhere: a verifiable \textsc{swhid}.}

\begin{table}[htbp]
  \centering\footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\linewidth}{@{}>{\raggedright\arraybackslash}p{2.4cm} >{\raggedright\arraybackslash}p{2.6cm} X >{\raggedright\arraybackslash}p{1.55cm} >{\raggedright\arraybackslash}p{1.9cm}@{}}
    \toprule
    \textbf{Field} & \textbf{Venue / body} & \textbf{Instrument} & \textbf{Mandatory?} & \textbf{URL} \\
    \midrule
    Economics \& social science & AEA journals & AEA Data Editor audit & Yes (2019) & \href{https://aeadataeditor.github.io}{\scriptsize\nolinkurl{aeadataeditor.github.io}} \\
    Economics \& social science & CNRS, HEC Paris, Université d'Orléans & \emph{cascad} certification (170+) & No (opt-in) & \href{https://cascad.tech}{\scriptsize\nolinkurl{cascad.tech}} \\
    Life sciences \& geography & GigaScience, Reproducible AGILE & CodeCheck & No (opt-in) & \href{https://codecheck.org.uk}{\scriptsize\nolinkurl{codecheck.org.uk}} \\
    Image processing & IPOL journal & Reference impl.\ + online demo & Yes (built-in) & \href{https://www.ipol.im/}{\scriptsize\nolinkurl{ipol.im}} \\
    Computer graphics & SIGGRAPH, TVCG, \ldots & Graphics Replicability Stamp (478)~\autocite{replicabilitystamp} & No (opt-in) & \href{http://www.replicabilitystamp.org/}{\scriptsize\nolinkurl{replicabilitystamp.org}} \\
    Security & USENIX Security & Artifact availability & Yes (2025) & \href{https://www.usenix.org/conference/usenixsecurity25}{\scriptsize\nolinkurl{usenix.org}} \\
    Cross-domain & ReScience C & Independent re-implementation & --- & \href{https://rescience.github.io}{\scriptsize\nolinkurl{rescience.github.io}} \\
    Pan-disciplinary & NISO & RP-31:2021 badging standard & --- & \href{https://www.niso.org/publications/rp-31-2021-badging}{\scriptsize\nolinkurl{niso.org}} \\
    \bottomrule
  \end{tabularx}
  \caption[Artifact evaluation beyond computer science]{Reproducibility and artifact-evaluation
  instruments across research fields. The instruments differ by discipline; what they share is the
  need for a durable, verifiable handle on the exact code under review --- the gap \textsc{ardc}
  fills with a \textsc{swhid}.}
  \label{tab:beyond-cs}
\end{table}

\section{Artifact evaluation with Software Heritage}

Look again at the first badge. \emph{Artifacts Available} asks for two things: a publicly accessible
archival repository, and a unique identifier for the object. For decades this has been met by the
legacy recipe --- a zip deposited on Zenodo, figshare, or in the ACM Digital Library, with a DOI. We
have already seen, at length, why that recipe is weak for \emph{source code}: a DOI certifies a
landing page rather than the bytes (Chapter~\ref{ch:identifiers}); the deposit can change, carries no
granularity, and discards the development history (Chapter~\ref{ch:foundations}). Everything the
badge really wants --- a durable archive and a unique, verifiable identifier --- is exactly what
\textsc{ardc} delivers: archive the living repository in Software Heritage, and reference it with a
\textsc{swhid}.

This is not a departure from policy; it is the rigorous way to follow it. Software Heritage is
already among the archives ACM accepts for the \emph{Available} badge, the \textsc{swhid} is now
ISO/IEC~18670, and the software-engineering community has explicitly asked that it be used for
artifact evaluation~\autocite{sigsoftaecsoftware2022,isoiec18670}. The gain is sharpest for the
\emph{reviewer}. A DOI gives a reviewer a link to follow and a record to trust; a \textsc{swhid}
gives something a DOI never can --- the ability to \emph{recompute the identifier from the bytes
under review and check that it matches}, offline, in under a minute. If it matches, the reviewer
holds cryptographic proof that the artifact in hand is exactly the artifact the authors named. If it
does not, that too is a finding.

\begin{handson}[Hands-on: verify an artifact as a reviewer]
\labmeta{$\sim$10 min \textperiodcentered{} needs a \textsc{swhid} tool $+$ the artifact files \textperiodcentered{} in-class, offline}
You are handed an artifact and the \textsc{swhid} the authors published for it.
\begin{enumerate}[nosep,leftmargin=*]
  \item Verify a file against its claimed content \textsc{swhid}: \texttt{swhid verify file.c
        swh:1:cnt:<hex>} --- it exits 0 only if the bytes match (Appendix~\ref{app:swhid-tools}).
  \item Re-compute the directory \textsc{swhid} of the whole artifact tree and compare it with the
        one in the paper. No archive, no network, and no trust required.
\end{enumerate}
\end{handson}

For the \emph{organiser}, the move is equally small: name Software Heritage in the call for
artifacts, and ask authors to put the qualified \textsc{swhid} of the precise artifact in the paper.
Each badge then maps cleanly onto a Software Heritage--native check:

\begin{table}[htbp]
  \centering\small
  \begin{tabularx}{\linewidth}{@{}lX@{}}
    \toprule
    \textbf{ACM badge} & \textbf{Software Heritage--native check} \\
    \midrule
    Artifacts Available     & archived in Software Heritage; a qualified \textsc{swhid} in the paper \\
    Evaluated --- Functional & the reviewer fetches the \emph{exact} \textsc{swhid}-pinned tree; no ``which zip?'' \\
    Evaluated --- Reusable   & \texttt{codemeta.json} + \texttt{CITATION.cff} + an \textsc{spdx} license, under that \textsc{swhid} \\
    Results Reproduced      & reproduction starts from a verifiable, immutable identifier \\
    \bottomrule
  \end{tabularx}
  \caption[Badges mapped to Software Heritage checks]{Each ACM badge mapped onto its Software Heritage--native check.}
  \label{tab:badge-crosswalk}
\end{table}

\takeaway{A reviewer can verify a \textsc{swhid} offline in a minute --- a DOI offers nothing of the
kind.}

\begin{callout}{Ready to paste: a call for artifacts}
\small For an organiser, here is language to drop into a call for artifacts, and a reviewer check:
\begin{quote}\itshape
Authors of accepted papers must archive the source code behind their results in Software Heritage
(\url{https://save.softwareheritage.org}) and include in the paper the \emph{qualified}
\textsc{swhid} of the precise artifact. A \textsc{doi} (e.g.\ via Zenodo, which forwards to Software
Heritage) may be supplied additionally for discoverability. For double-blind submission, give a
\emph{directory} \textsc{swhid}, which reveals no author or origin.
\end{quote}
\textbf{Reviewer check (offline, $<$1 min):} resolve the \textsc{swhid} at
\texttt{archive.softwareheritage.org/<swhid>}; recompute it from the bytes (\texttt{swhid verify});
confirm it pins the \emph{exact} code the claims rest on. A mismatch is itself a finding. \emph{When
it doesn't fit:} for artifacts that cannot be public (licensed data, large binaries, special
hardware), require the public part archived and the rest escrowed --- Software Heritage solves \emph{identity and
availability}, not buildability, which \emph{Functional}/\emph{Reusable} still test by running.
\end{callout}

\section{Double-blind review, and dense pinpointing}

Artifact evaluation often happens under \emph{double-blind} review --- where reviewers do not know
who the authors are, nor the authors who their reviewers are --- which seems to pose a problem: how
do you share your code without revealing who wrote it? Tools that rewrite a repository's history
to anonymise it are fragile, and they leak. The intrinsic identifier sidesteps the whole difficulty.
A \emph{directory} \textsc{swhid} is computed from content alone --- no author, no origin, no commit
metadata, nothing to scrub --- and it works for any technology, not just Git. Submit the
\texttt{dir} \textsc{swhid}; reviewers confirm the bytes are exactly yours while learning nothing
about you.

The same idea scales to a paper that pinpoints code throughout its body. Modern papers already do
this densely: \emph{Source Code Hotspots} ties its findings to exact passages, naming each with a
qualified \textsc{swhid} so a reader can open the precise lines in the
archive~\autocite{spinellis2026hotspots}. And the part that carries \emph{provenance} is precisely
the qualifiers --- \texttt{origin}, \texttt{visit}, \texttt{anchor} --- while the core hash, the
identifier proper, reveals nothing about authorship. So the \emph{only} difference between the
double-blind submission and the camera-ready version (the final, de-anonymised paper) is removing,
then restoring, those qualifiers: the core
hashes are identical, and every pinpoint a reviewer checked during blind review stays valid, to the
character, in the final paper. One mechanical, fully reversible edit (Figure~\ref{fig:doubleblind})
--- no history-rewriting, no risk of a leak.

\begin{figure}[htbp]
  \centering
  \resizebox{\linewidth}{!}{%
  \begin{tikzpicture}[font=\sffamily\footnotesize,
    core/.style={draw=swhdark, fill=swhaccent!85, text=white, rounded corners=1pt, minimum height=0.75cm, inner xsep=5pt},
    qual/.style={draw=swhdark!55, fill=swhbg, rounded corners=1pt, minimum height=0.75cm, inner xsep=5pt}]
    \node[core] (b) {\ttfamily swh:1:cnt:43a6b232\ldots};
    \node[font=\itshape, above=1.5mm of b] {double-blind: core only};
    \node[core, right=3.4cm of b] (c) {\ttfamily swh:1:cnt:43a6b232\ldots};
    \node[qual, right=-0.4pt of c] (q) {\ttfamily ;origin=\ldots;anchor=\ldots};
    \node[font=\itshape, above=1.5mm of c] {camera-ready: core $+$ qualifiers};
    \draw[{Stealth[]}-{Stealth[]}, swhdark, thick] (b.east) -- node[above=-1pt, font=\scriptsize] {restore} node[below=-1pt, font=\scriptsize] {strip} (c.west);
  \end{tikzpicture}}
  \caption[Double-blind in one reversible edit]{The core hash (red) is identical on both sides and
  reveals nothing about authorship; only the locating qualifiers are stripped for blind review and
  restored for the camera-ready version. Every pinpoint a reviewer checks survives the round trip
  unchanged.}
  \label{fig:doubleblind}
\end{figure}

\takeaway{Double-blind vs camera-ready: strip, then restore, the qualifiers --- the content hash
never changes.}

\begin{handson}[Hands-on: a double-blind artifact]
\labmeta{$\sim$10 min \textperiodcentered{} needs a \textsc{swhid} tool \textperiodcentered{} in-class, offline}
Compute the directory \textsc{swhid} of your artifact tree (\texttt{swhid dir ./}; see
Appendix~\ref{app:swhid-tools}) and submit \emph{that}. It names your exact bytes and nothing else.
After acceptance, archive the repository and add the \texttt{origin}/\texttt{anchor} qualifiers ---
the same identifier, now pointing home.
\end{handson}

\section{Built into the record of science}

Best of all is when none of this is left to the author's goodwill. Since November 2020 the journal
\emph{eLife} has archived the code behind every article in Software Heritage automatically, embedding
the \textsc{swhid} in the article's machine-readable metadata and validating it before
publication~\autocite{elifeswh2020}. Availability stops being a promise a reviewer checks once, and
becomes a durable property of the published record itself --- which is, in the end, exactly where it
belongs.

That is the case for building artifact evaluation on Software Heritage. What remains is to look
outward: at how widely this is already being adopted, the policy that now backs it, and the larger
mission it serves. That is the final chapter.
